# Use a slim Python base image for smaller size
FROM python:3.12-slim-buster
WORKDIR /app

# Copy the requirements file and install dependencies
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application code
COPY . .

# Expose the port where the AI application will run (e.g., Uvicorn)
EXPOSE 8000

# Command to run the application (assuming main:app for Uvicorn/FastAPI)
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]

# Optional: Add a healthcheck if your application has a health endpoint
# HEALTHCHECK --interval=5m --timeout=3s CMD curl -f http://localhost:8000/health || exit 1
